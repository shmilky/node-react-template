'use strict';

const landing = {
    HOME_PAGE: '/',
};

const demoPages = {
    DEMO_PAGE: '/demo-path',
};

const modalNames = {
    DEMO_MODAL: 'DEMO_MODAL'
};

module.exports = {
    landing: landing,
    demoPages: demoPages,
    modalNames: modalNames
};