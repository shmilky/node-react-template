'use strict';

import React from 'react';

class AppForSSR extends React.Component {
    render () {
        return (
            <div>
                SSR is not working yet
            </div>
        );
    }
}

export default AppForSSR;